
import { navTriggerContainer } from 'dustin-jwalker/helpers/nav-trigger-container';
import { module, test } from 'qunit';

module('Unit | Helper | nav trigger container');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = navTriggerContainer([42]);
  assert.ok(result);
});

