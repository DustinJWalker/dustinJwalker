
import { navContainer } from 'dustin-jwalker/helpers/nav-container';
import { module, test } from 'qunit';

module('Unit | Helper | nav container');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = navContainer([42]);
  assert.ok(result);
});

